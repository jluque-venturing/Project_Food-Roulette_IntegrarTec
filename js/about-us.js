import { initChrome } from './ui.js';
import { initLang } from './lang.js';

// Navbar + footer + modal de login + tema + auth (todo compartido desde ui.js)
initChrome({ active: 'about' });
initLang();
