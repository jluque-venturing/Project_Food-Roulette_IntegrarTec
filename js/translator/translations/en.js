// English is the base language — no dictionary needed.
// All keys used in t() are written directly in English in the HTML and JS,
// so t('Home') returns 'Home' automatically as fallback when this language is active.
// This file is never loaded by the app; it exists only as documentation.
export default {};
